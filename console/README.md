# Deprecated — merged into `frontend/`

Platform admin UI now lives at `/platform` and `/platform/packages` in the main frontend app.
Do not run or deploy this package. Use `npm run dev:frontend` / the single frontend build instead.
