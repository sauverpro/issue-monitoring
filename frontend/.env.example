# ── Backend API URL ─────────────────────────────────────────────────────────────
# Your Fly.io backend URL — set this in Vercel's project environment variables
# Leave empty to use the Vite dev proxy (local development)
VITE_API_BASE=https://issue-monitoring-api.fly.dev
